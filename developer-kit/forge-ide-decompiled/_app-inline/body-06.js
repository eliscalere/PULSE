        // Runtime feature flags consumed by Prometheus (set before athenaAgent.js loads).
        // Example override:
        // window.Forge_FLAGS = { hidePublicApiProviders: true };
        window.Forge_FLAGS = window.Forge_FLAGS || {};
    
